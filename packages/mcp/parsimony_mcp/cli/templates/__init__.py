"""Prose templates loaded by the init wizard's mergers.

Files in this package are plain text with ``str.format_map``
placeholders. Kept as package data so ``importlib.resources``
locates them regardless of install layout (editable vs wheel).
"""
